"""Module D — Composite stress index.

JRC handbook methodology applied to the events table. Pure functions per layer
(aggregation, normalization, scoring) so each step is independently testable.
"""
