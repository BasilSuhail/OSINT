"""WS-D coverage-bias table — per-country attention baselines.

Measures how unevenly countries are covered in the event data and publishes
per-country monthly-volume baselines so scoring can judge each country against
its own history. Pure stats layer + one-shot CLI, mirroring `app/labels/`.
"""
