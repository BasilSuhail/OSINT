name: buildExpected

on:
  pull_request:
  push:
    branches: [main]

jobs:
  compatibility:
    name: buildExpected compatibility
    runs-on: ubuntu-latest
    steps:
      - name: buildExpected compatibility check
        run: |
          echo "buildExpected compatibility placeholder"
